<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>
    <groupId>com.banca</groupId>
    <artifactId>banca-parent</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>
    <name>Banca Nacional - Parent POM</name>
    <description>
        Stack: Java 8 + WildFly 10.1.0.Final + MySQL 5.7
        Empaquetado: EAR (EJB + WAR)
    </description>

    <modules>
        <module>banca-ejb</module>
        <module>banca-web</module>
        <module>banca-ear</module>
    </modules>

    <properties>
        <!-- Java 8 obligatorio -->
        <maven.compiler.source>1.8</maven.compiler.source>
        <maven.compiler.target>1.8</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- WildFly 10.1 implementa Java EE 7 -->
        <javaee.version>7.0</javaee.version>

        <!--
            MySQL Connector/J 5.1.49
            - Compatible con MySQL 5.7
            - Compatible con Java 8
            - NO usar 8.x (requiere serverTimezone, distinto driver class)
        -->
        <mysql.version>5.1.49</mysql.version>

        <log4j.version>2.17.2</log4j.version>
        <jbcrypt.version>0.4</jbcrypt.version>
        <jackson.version>2.13.5</jackson.version>
        <jstl.version>1.2</jstl.version>
    </properties>

    <dependencyManagement>
        <dependencies>

            <!-- Java EE 7 - provisto por WildFly 10.1, scope provided -->
            <dependency>
                <groupId>javax</groupId>
                <artifactId>javaee-api</artifactId>
                <version>${javaee.version}</version>
                <scope>provided</scope>
            </dependency>

            <!--
                MySQL Connector/J 5.1.49
                scope: provided en EJB (lo instalamos como módulo en WildFly)
                Así WildFly gestiona el pool de conexiones
            -->
            <dependency>
                <groupId>mysql</groupId>
                <artifactId>mysql-connector-java</artifactId>
                <version>${mysql.version}</version>
                <scope>provided</scope>
            </dependency>

            <!-- Log4j2 - compatible con Java 8 -->
            <dependency>
                <groupId>org.apache.logging.log4j</groupId>
                <artifactId>log4j-api</artifactId>
                <version>${log4j.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.logging.log4j</groupId>
                <artifactId>log4j-core</artifactId>
                <version>${log4j.version}</version>
            </dependency>

            <!-- BCrypt para contraseñas -->
            <dependency>
                <groupId>org.mindrot</groupId>
                <artifactId>jbcrypt</artifactId>
                <version>${jbcrypt.version}</version>
            </dependency>

            <!-- JSTL para vistas JSP -->
            <dependency>
                <groupId>javax.servlet</groupId>
                <artifactId>jstl</artifactId>
                <version>${jstl.version}</version>
            </dependency>

            <!-- Referencias internas entre módulos -->
            <dependency>
                <groupId>com.banca</groupId>
                <artifactId>banca-ejb</artifactId>
                <version>${project.version}</version>
                <type>ejb</type>
            </dependency>
            <dependency>
                <groupId>com.banca</groupId>
                <artifactId>banca-web</artifactId>
                <version>${project.version}</version>
                <type>war</type>
            </dependency>

        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.8.1</version>
                    <configuration>
                        <source>1.8</source>
                        <target>1.8</target>
                        <encoding>UTF-8</encoding>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-ejb-plugin</artifactId>
                    <version>3.1.0</version>
                    <configuration>
                        <ejbVersion>3.2</ejbVersion>
                        <generateClient>true</generateClient>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-war-plugin</artifactId>
                    <version>3.3.2</version>
                    <configuration>
                        <failOnMissingWebXml>true</failOnMissingWebXml>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-ear-plugin</artifactId>
                    <version>3.3.0</version>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

</project>
